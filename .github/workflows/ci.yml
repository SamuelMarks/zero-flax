name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        path: 'zero-flax'

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.12'

    - name: Install Dependencies
      working-directory: zero-flax
      run: |
        python -m pip install --upgrade pip
        pip install ruff pytest pytest-cov build
        if [ -f "pyproject.toml" ]; then pip install -e .[test] || true; fi
        if [ -f "requirements.txt" ]; then pip install -r requirements.txt || true; fi

    - name: Check Code Formatting (Ruff)
      working-directory: zero-flax
      run: ruff format --check .

    - name: Check Linting (Ruff)
      working-directory: zero-flax
      run: ruff check .

    - name: Test & Coverage
      working-directory: zero-flax
      run: |
        pytest --cov=. --cov-report=xml
        
    - name: Compliance Check
      working-directory: zero-flax
      run: |
        pip install flax ml_framework_snapshots
        ml_framework_snapshots check flax src/zero_flax --reference-prefix flax --target-prefix zero_flax
        
    - name: Update Badges
      working-directory: zero-flax
      run: |
        python scripts/update_badges.py
        if ! git diff --exit-code README.md; then
          echo "Coverage badges in README.md are out of date. Please run pre-commit or the update badges script locally and commit the changes."
          exit 1
        fi

    - name: Build Package
      working-directory: zero-flax
      run: python -m build
